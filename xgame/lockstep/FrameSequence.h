//------------------------------------------------------------------------
// * @filename: FrameSequence.h
// *
// * @brief: 帧序列
// *
// * @author: XGM
// * @date: 2019/01/04
//------------------------------------------------------------------------
#pragma once

#include <unordered_map>

class Frame;

class FrameSequence
{
public:
	~FrameSequence();

	//------------------------------------------------------------------------
	// 添加帧
	//------------------------------------------------------------------------
	bool AddFrame(Frame* pFrame);

	//------------------------------------------------------------------------
	// 当前帧
	//------------------------------------------------------------------------
	Frame* GetFrame(int32_t nFrameID);

	//------------------------------------------------------------------------
	// 获得帧数
	//------------------------------------------------------------------------
	uint32_t GetFrameSize();

private:
	// 帧列表
	std::unordered_map<int32_t, Frame*> m_Frames;
};
